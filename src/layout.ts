// Vertical padding for the app header.
// Shared with the Sidebar so the tab button stays vertically aligned.
export const HEADER_V_PADDING = 'pt-6 pb-4'

// Approximate height of the TonalitySelector block (py-1.5 row + mb-6).
// Used to offset the sidebar content below header + selector.
// Update if the selector's vertical size changes significantly.
export const SELECTOR_BLOCK_H = 'py-1.5 mb-6'
